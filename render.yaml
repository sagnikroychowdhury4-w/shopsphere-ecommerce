services:
  - type: web
    name: shopsphere-reviews-api
    env: docker
    plan: free
    autoDeploy: true
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: shopsphere-db
          property: connectionString
    healthCheckPath: /health
databases:
  - name: shopsphere-db
    plan: free
